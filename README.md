# Hackaton-Battery-Passport

Sur VS Code : 

```
git clone https://github.com/lisacharuel/Hackaton-Battery-Passport.git
cd Coding-Agent-Safran
code .
```

Ensuite on modifie le code puis on push sur une branche perso : 

```
git checkout -b nom_branche
git add .
git commit -m "Ajout de fonctionnalité blabla"
git push -u origin nom_branche

```
